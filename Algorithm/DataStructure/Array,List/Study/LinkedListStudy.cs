using System;
using System.Collections.Generic;
using System.Text;

namespace Algorithm.Array_List
{
    internal class LinkedListStudy
    {
        public void Test()
        {
            LinkedList<int> list = new LinkedList<int>();
        }

        // < List의 시간복잡도 >
        // 접근       탐색      삽입      삭제
        // O(1)       O(n)      O(n)      O(n)

        // < LinkedList의 시간복잡도 >
        // 접근       탐색      삽입      삭제
        // O(n)       O(n)      O(1)      O(1)

    }
}
